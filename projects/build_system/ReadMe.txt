Build system with using Makefile language:
- Include 2 main makefiles: "common.mk" and "Makefile"
- With each project, you need to copy Makefile.template into your project folder and remove extension of file. You update list of files/folders and special setting for your project.